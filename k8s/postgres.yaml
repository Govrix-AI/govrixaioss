apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: agentland
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: timescale/timescaledb:latest-pg16
        env:
        - name: POSTGRES_DB
          value: agentland
        - name: POSTGRES_USER
          value: agentland
        - name: POSTGRES_PASSWORD
          value: agentland
        ports:
        - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: agentland
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
