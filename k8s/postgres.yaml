apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: govrix
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: timescale/timescaledb:latest-pg16
        env:
        - name: POSTGRES_DB
          value: govrix
        - name: POSTGRES_USER
          value: govrix
        - name: POSTGRES_PASSWORD
          value: govrix
        ports:
        - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: govrix
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
